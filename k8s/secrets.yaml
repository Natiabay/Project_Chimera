# Template only — do not commit real values. Use sealed-secrets or external secret operator in production.
apiVersion: v1
kind: Secret
metadata:
  name: chimera-secrets
  namespace: chimera
type: Opaque
stringData:
  POSTGRES_URL: "postgresql://user:REPLACE@postgres:5432/chimera"
  REDIS_URL: "redis://redis:6379/0"
  WEAVIATE_URL: "http://weaviate:8080"
  WEAVIATE_API_KEY: "REPLACE"
  GEMINI_API_KEY: "REPLACE"
  GOOGLE_API_KEY: "REPLACE"
  NEWSDATA_API_KEY: "REPLACE"
  # Optional: COINBASE_API_KEY, TWITTER_BEARER_TOKEN, etc.
